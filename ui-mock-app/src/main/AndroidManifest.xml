<?xml version="1.0" encoding="utf-8"?>
<!--
  Standalone mock shell (#2636 slice D16).

  Deliberately minimal: no permissions (the mock performs no I/O — the reducer
  is pure), no services, no providers, no backup. The activity renders the
  deterministic mock state through the real shared screens; every "send",
  "connect" or "generate" it exposes mutates local state only.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketShellUiMock">

        <activity
            android:name=".MockMainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
