{
  "schema": 1,
  "testFiles": {
    "tests/unit/appSettings.test.ts": {
      "suite": "mobile app settings",
      "tests": [
        "reads and persists shared appearance, connection, and dictation preferences",
        "degrades malformed or unsupported saved values to safe defaults",
        "clamps terminal size using the pinned desktop font-size policy",
        "normalizes valid language hints and applies the adapter silence bounds"
      ]
    },
    "tests/unit/dictationSettingsInteraction.test.ts": {
      "suite": "mounted dictation settings controls",
      "tests": [
        "binds the production Voice template to accessible persisted language and silence controls"
      ]
    },
    "tests/unit/homeSurface.test.ts": {
      "suite": "phone workspace destinations",
      "tests": [
        "starts at connection setup and opens sessions after a connection is ready",
        "returns to the live terminal after a session is attached",
        "keeps host setup and session selection reachable from the live terminal",
        "backs from the terminal to sessions, then to connection details",
        "returns to connection setup when disconnected",
        "closes nested routes before stepping back through a connected workspace"
      ]
    },
    "tests/unit/buildDiagnostics.test.ts": {
      "suite": "packaged build diagnostics",
      "tests": [
        "imports and executes the real formatter from the pinned core source",
        "imports theme and font policy from the pinned shared UI source",
        "accepts a manifest only when both pinned source revisions and exact bundle bytes match",
        "fails closed when the recorded core source revision differs",
        "fails closed when the recorded shared UI source revision differs",
        "fails closed when the packaged asset bytes differ from the manifest",
        "fails closed when a bundle asset is missing"
      ]
    },
    "tests/unit/sshCapabilityBridge.test.ts": {
      "suite": "Capacitor SSH resource snapshot bridge",
      "tests": [
        "wraps the core request ID in Capacitor method options",
        "rejects a mismatched request ID instead of accepting an unverified snapshot",
        "rejects a missing counter instead of accepting an unverified snapshot",
        "rejects a negative counter instead of accepting an unverified snapshot",
        "propagates a native bridge rejection"
      ]
    },
    "tests/unit/composerDelivery.test.ts": {
      "suite": "composer delivery adapter and per-target drafts",
      "tests": [
        "retains an ambiguous draft and does not replay it after reconnect",
        "treats an acknowledgement that arrives after transport loss as uncertain and stops the paste",
        "keeps one session draft separate from another and clears only after acknowledged delivery"
      ]
    },
    "tests/unit/platformInput.test.ts": {
      "suite": "Android platform input adapter",
      "tests": [
        "reads selected documents in ordered bounded chunks and returns exact attachment bytes",
        "preserves valid files when one selection is over the shared native transfer limit",
        "rejects mismatched chunk identity and still releases the native document reference",
        "materializes incoming shared text and files before delivering the share callback",
        "registers before starting speech, filters foreign events, and stops the matching request"
      ]
    },
    "tests/unit/speechRecognition.test.ts": {
      "suite": "Android speech option boundary",
      "tests": [
        "uses the 4-second silence default and preserves the explicit auto sentinel",
        "rounds and clamps the silence window to the supported 2-to-60-second range",
        "keeps a trimmed BCP-47 hint and defaults malformed untyped values safely"
      ]
    },
    "tests/unit/destinationInventory.test.ts": {
      "suite": "pre-rewrite destination inventory",
      "tests": [
        "maps each shipped destination to a replacement route, owning issue, and implementation status",
        "states which prior settings/support surfaces have working routes in this slice"
      ]
    },
    "tests/unit/dictationStartCancellation.test.ts": {
      "suite": "pending dictation start cancellation",
      "tests": [
        "stops a pending start once when the mounted composer backgrounds"
      ]
    },
    "tests/unit/diagnostics.test.ts": {
      "suite": "local diagnostics",
      "tests": [
        "stores a bounded event log and normalizes untrusted event metadata",
        "exports only reviewed, normalized diagnostic fields"
      ]
    },
    "tests/unit/installedDataMigration.test.ts": {
      "suite": "installed Android data migration",
      "tests": [
        "maps legacy settings only when the JS settings are absent and preserves existing fields",
        "stages complete history, trust and asset bytes before marking the import complete",
        "repairs missing JS settings and trust pins from a completed import without rereading native data",
        "preserves newer JS settings and skips the startup reload when no setting was written",
        "requests only one reload in the current startup session after writing imported settings",
        "refuses malformed sync state without staging or marking partial data complete",
        "imports unrelated data as partial and visibly retains an unavailable encrypted store",
        "shows an unmappable legacy terminal size on first import and completed-record startup",
        "marks decrypted native-only credentials partial and offers only opaque legacy host key references",
        "connects a saved host through an opaque native key reference and keeps passphrases transient",
        "does not mark complete or write settings when an imported asset hash differs",
        "refuses conflicting JS trust pins without replacing either fingerprint"
      ]
    },
    "tests/unit/navigation.test.ts": {
      "suite": "mobile route stack",
      "tests": [
        "keeps nested settings and support routes reachable through Back",
        "retains a selected report ID and resets the stack when returning home",
        "clears the selected event when opening the whole-log export preview",
        "does not push the current destination a second time"
      ]
    },
    "tests/unit/filesWorkspace.test.ts": {
      "suite": "SFTP file workspace policy and byte-I/O adapter",
      "tests": [
        "resolves paths under the configured remote root and rejects traversal and NUL",
        "lists valid children with shared directory-first ordering and name classification",
        "rejects unsafe names returned by native SFTP listing",
        "rejects duplicate child names instead of selecting an ambiguous remote path",
        "reads bounded bytes and combines filename and magic classification",
        "rejects oversized files before reading and detects a size change during bounded reads",
        "refuses to save when metadata changes immediately before the native conditional write",
        "saves UTF-8 edits only through the native conditional write when metadata still matches",
        "fails closed on a symlink entry before reading or writing",
        "fails closed when the native listing omits a regular-file type",
        "does not offer binary magic content to the text editor",
        "uploads bytes through a resolved path and prevents implicit overwrite",
        "stages sanitized attachment names, preserves partial success, and returns retention candidates",
        "does not return completed uploads as attachments after cancellation",
        "leaves the remote tree untouched for an empty attachment batch",
        "rejects files above the native transfer limit before issuing a write",
        "rejects stale generations before requests and after in-flight responses",
        "rejects native responses with an unrelated request ID"
      ]
    }
  }
}
